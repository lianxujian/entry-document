1、process.argv 数组后写内容添加数组元素[node.exe的路径,本文件路径]
2、process.stdout.write 输出
3、process.stdin.on 输入
4、__dirname;__filename;
5、process.exit() 退出程序


// cls 清屏; dir 显示目录; cd/ cd.. cd ;
// 进数据库 mysql -uroot -p;show databases;use 库名;show table;desc 表名;

//修改入口文件 npm init
//上传(发布) npm adduser
//重新发布   npm publish (--tag 1.2.3)
//删除		 npm unpublish@1.0.0
//下载 npm install demo1412b(模块儿名字)
//删除 npm uninstall 模块儿名字
//显示下载路径 npm root
//下载;显示绝对路径 npm install -g; npm root -g;

//注释文件：README.md
//设置字符编码：chcp 65001;
//清除缓存：npm cache clear
//设置全局路径 npm set prefix